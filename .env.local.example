# Convex — copy this to .env.local and fill in the values
# Run: npx convex dev  (to get NEXT_PUBLIC_CONVEX_URL and set up JWT keys)

NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Gemini API key — stored as a Convex secret (NOT here)
# Run: npx convex env set GEMINI_API_KEY=your-key-here
